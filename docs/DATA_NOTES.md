# DATA_NOTES

Generated by `moneygraph.quality.check_data` on each pipeline run. Numbers below are
measured from the real data in `data/`, not asserted in advance.

- nodes: 2248, edges: 3119, transactions: 4840, seeds: 81
- depth distribution: {'0': 81, '1': 472, '2': 462, '3': 789, '4': 444}
- seeds without any observed outflow: 31
- depth-4 nodes with no outflow (censored, traversal artifact — never call these terminal): 444
- nodes where out_sum > in_sum (likely unobserved inflow, not an error): 377
- transaction dates carry a time component: False (if False, same-day ordering within a date is unknown — temporal features are still computed on day granularity)
- self-loops: 0 in edges, 0 in transactions (excluded from the graph)
- edges vs transactions reconciliation: sum(n_tx) - count(transactions) = 0; sum(edges.sum_kzt) - sum(transactions.sum_kzt) = 0.0
- weakly connected components on full node graph: 35
- weakly connected components on edge-only subgraph: 16

Known data limitations (see AGENTS.md): transfers below 5000 KZT are excluded from this export (an export threshold, not an AML threshold); the data covers July only, so inflow on the last days of the month may not yet have moved on (right-edge censoring, guarded by late_inflow_share); inflow is always a lower bound and seed inflow is essentially unobserved.