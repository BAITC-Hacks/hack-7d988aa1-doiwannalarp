import {ResponsiveContainer,BarChart,Bar,XAxis,YAxis,Tooltip,CartesianGrid,Legend} from 'recharts';
import {activity,money} from '../../lib/data';
import type {Transaction} from '../../types';
import {EmptyState} from './index';
export function ActivityChart({transactions,gid}:{transactions:Transaction[];gid?:string}){const rows=activity(transactions,gid);if(!rows.length)return <EmptyState title="Нет наблюдаемых переводов"/>;return <div className="chart"><ResponsiveContainer width="100%" height="100%"><BarChart data={rows} margin={{top:12,right:16,left:6,bottom:0}}><CartesianGrid vertical={false} stroke="#E4EAF2"/><XAxis dataKey="date" tickFormatter={v=>v.slice(8,10)+'.'+v.slice(5,7)} tick={{fontSize:11}} axisLine={false} tickLine={false}/><YAxis tickFormatter={v=>new Intl.NumberFormat('ru-RU',{notation:'compact'}).format(v)} tick={{fontSize:11}} axisLine={false} tickLine={false} width={62}/><Tooltip formatter={v=>money(v as number)} labelFormatter={v=>String(v)}/>{gid?<><Legend iconType="circle"/><Bar isAnimationActive={false} name="Поступления, KZT" dataKey="inflow" fill="#168477" radius={[2,2,0,0]}/><Bar isAnimationActive={false} name="Отправления, KZT" dataKey="outflow" fill="#356DF3" radius={[2,2,0,0]}/></>:<Bar isAnimationActive={false} name="Переводы, KZT" dataKey="total" fill="#356DF3" radius={[2,2,0,0]}/>}</BarChart></ResponsiveContainer></div>}
